---
teleport-cluster:
  podSecurityPolicy:
    enabled: false
  clusterName: teleport.suslian.engineer
  highAvailability:
    certManager:
      enabled: true
      issuerName: letsencrypt-prod
      issuerKind: ClusterIssuer
